export const PULL_THRESHOLD = 80; // Threshold for refresh activation
export const MIN_PULL_DISTANCE = 50; // Minimum pixels to move before pull is considered
export const SCROLL_TOP_TOLERANCE = 5; // Allow pull if scrolled slightly
export const PULL_INDICATOR_MAX_HEIGHT_ADDITION = 40; // Max additional height for the indicator beyond threshold
export const PULL_DISTANCE_DAMPENING_FACTOR = 0.4; // Dampening factor for pull distance
export const MIN_PULL_DISPLAY_THRESHOLD = 20; // Minimum pull distance to show the static spinner icon
export const PULL_OPACITY_THRESHOLD_DIVISOR = 2; // Divisor for threshold to calculate opacity range
